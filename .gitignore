# Environment files (never commit secrets)
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
supabase/.temp/

# Prevent large image files from cluttering the repo root
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# TypeScript incremental build cache
*.tsbuildinfo

# Deno lockfile from local `deno check` runs on supabase/functions/**
deno.lock

# Local training-data export snapshots (scripts/export-training-data.mjs)
training-exports/

# Scratch commit-message file used by the agent workflow; never part of the tree.
.git-commit-msg.txt
